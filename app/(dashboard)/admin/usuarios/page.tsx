import { requirePageRole } from '@/lib/auth-page';
export const dynamic='force-dynamic';
export default async function Page(){const {supabase}=await requirePageRole(['admin']);const {data,error}=await supabase.from('users').select('id,email,full_name,phone,role').order('created_at',{ascending:false}).limit(500);if(error)throw new Error('No se pudieron cargar los usuarios.');return <div className="space-y-5"><h1 className="text-2xl font-bold">Usuarios</h1><p className="text-sm text-muted-foreground">Hasta 500 cuentas recientes. Los cambios de rol requieren el procedimiento privilegiado de alta de personal.</p><div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr>{['Nombre','Correo','Teléfono','Rol'].map(h=><th className="p-3" key={h}>{h}</th>)}</tr></thead><tbody>{data?.map((u:{id:string;full_name:string|null;email:string;phone:string|null;role:string})=><tr className="border-t" key={u.id}><td className="p-3">{u.full_name||'Sin nombre'}</td><td>{u.email}</td><td>{u.phone||'—'}</td><td>{u.role}</td></tr>)}</tbody></table></div></div>;}
