// Demonstration fixtures must never overwrite a real catalog or inventory.
console.error('El seed remoto fue retirado para proteger datos reales. Usa npm run dev:demo para una copia local del catálogo de demostración, o carga tus productos propios desde Administración.');
process.exitCode=1;
