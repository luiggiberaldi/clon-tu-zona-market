'use client';
import { useState } from 'react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';
export function DemoContact(){const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');async function send(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage('');const form=e.currentTarget;const f=new FormData(form);try{const response=await fetch('/api/demo/control',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'help',values:{name:f.get('name'),email:f.get('email'),message:f.get('message')}})});const r=await response.json();if(r.error)throw new Error(r.error.message);setMessage('Consulta local registrada: '+r.data.id+'. Puedes comprobarla en el buzón.');form.reset();}catch(e){setMessage(e instanceof Error?e.message:'No se pudo enviar.');}finally{setBusy(false);}}
return <div className="space-y-4"><p className="text-sm">Este formulario guarda una consulta de prueba y la entrega al buzón local. No contacta a TuZonaMarket ni envía correos reales.</p><form className="space-y-3" onSubmit={e=>void send(e)}><label className="block text-sm">Nombre de prueba<input name="name" minLength={2} maxLength={100} required className="mt-1 block w-full rounded border p-2"/></label><label className="block text-sm">Correo de prueba<input name="email" type="email" required className="mt-1 block w-full rounded border p-2"/></label><label className="block text-sm">Tu consulta<textarea name="message" minLength={10} maxLength={2000} required className="mt-1 block w-full rounded border p-2"/></label><Button disabled={busy}>Enviar consulta local</Button></form>{message&&<p role="status" className="text-sm">{message}</p>}<Link className="text-primary underline" href="/demo/buzon">Abrir buzón local</Link></div>}
